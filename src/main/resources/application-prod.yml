spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yupao
    username: root
    password: 1234
  # session 失效时间
  session:
    timeout: 86400
server:
  address: 0.0.0.0

